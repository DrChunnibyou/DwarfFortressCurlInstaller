# DwarfFortressCurlInstaller
Written in C using libcurl to scrape the bay12games classic dwarf fortress downloads. It finds the latest available build and downloads it via curl.

To compile, simply type make in the cloned repository, then run the created executable 

**Currently only works for linux
**Curl is required to be installed in your system
